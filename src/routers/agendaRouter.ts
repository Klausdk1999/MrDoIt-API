import { Router } from "express";
//import { recommendationController } from "../controllers/recommendationController.js";

const agendaRouter = Router();

agendaRouter.post("/schedule",);
agendaRouter.get("/",);

export default agendaRouter;
